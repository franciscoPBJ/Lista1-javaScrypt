var A = Number.parseInt(window.prompt('informe um numero'));
var B = Number.parseInt(window.prompt('informe outro numero'));

if(A == B){
    var soma = A + B;
    window.alert(`a soma é ${soma}`);
}
else{
    var C = A*B;
    window.alert(`o valor da variavel C é ${C}`);
}